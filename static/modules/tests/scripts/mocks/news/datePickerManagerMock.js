define(['knockout','moment','domManager','newsConstants','newsEventListeners', 'itemsController'],
function(ko, Moment, domManager, constants, newsEventListeners, controller)
{
	function initDatePicker(newsKey, controller)
    {
    }

    function refreshDatePicker(newsKey, selectedDate)
    {
    }

    return {
        init: initDatePicker,
        refresh: refreshDatePicker
    };
});